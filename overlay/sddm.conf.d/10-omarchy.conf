# Keep SDDM on the X greeter by default for maximum GPU compatibility.
# The packaged `sddm` theme is used; upstream ships its own theme which
# requires Qt6 SDDM builds — see docs/LIMITATIONS.md.
[General]
DisplayServer=x11

[Theme]
Current=breeze
